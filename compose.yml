services:
  dbservice:
    image: mysql:latest
    container_name: jobportaldb
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: jobportal
    volumes:
      - /Users/pugal/Documents/Jobportal-data:/var/lib/mysql
